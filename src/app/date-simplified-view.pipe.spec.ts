import { DateSimplifiedViewPipe } from './date-simplified-view.pipe';

describe('DateSimplifiedViewPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSimplifiedViewPipe();
    expect(pipe).toBeTruthy();
  });
});
